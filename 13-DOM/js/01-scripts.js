/* Uno de los temas más importantes en JavaScript que debes dominar como desarrollador@ DOM. 
   No le recomiendo para nada buscar la definición en Wikipedia porque ni ellos mismo s lo entienden
   Los conceptos son errados*/
   
   /** Pero es más sencill de lo que parece. El documento es el objeto principal del DOM y también hay algo llamado
    * root que es el HTML. De ahí see divide en 2 partes. Con el DOM podemos seleccionar elementos del html, crea html, modificar html
    * eventos entre otros.
    */

   let elemento;

   //elemento = document;
   //element = document.all;
   //element = document.head;
   //element = document.body;
   //element = document.URL;
   //element = document.characterSet;
   //element = document.forms;
   //element = document.forms[0].id;
   //element = document.forms[0].method;
   //element = document.forms[0].action;
   //element = document.links;
   //element = document.links[4].id;
   //element = document.links[4].className;
   //element = document.links[4].classList;
   //element = document.links[4].classList.length;

   //element = document.images;
   //element = document.scripts;
   element = document.scripts[0];

   console.log(element);

   //const paragraph = document.querySelectorAll("p");

   //alert(paragraph[0]);