/**Vamos a seleccionar por  su ID
   recuerda los ID solo se deben utilizar uno  con el mismo nombre
   */

const formulario = document.getElementById('formulario');
console.log(formulario);

const noExiste = document.getElementById('no-existe');
console.log(noExiste); //si no existe estará vacio

//Añadir un ID 2 veces, seleccionará el primero que encuentre


