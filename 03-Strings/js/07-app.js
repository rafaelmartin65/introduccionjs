//toUpperCase te va a permitir cambiar un texto a mayúsculas.
const producto = "Televisor 55 Pulgadas";
console.log(producto.toUpperCase());
//toLowerCase cambia el texto a minúscula.
console.log(producto.toLowerCase());

//toString: Convierte la variable en texto.
const cantidad = 200;
console.log(cantidad);
console.log(cantidad.toString());