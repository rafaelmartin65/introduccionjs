// vamos a ver el 3er tipo de declaración de variables -- const ---
// existen pocas diferencias ente el let y el const
const producto = 'Monitor de 30 pulgadas';
console.log(producto);

// Deben inicarse con un valor y son inmutables
const precio = 200;

// precio = 300;  *** no se puede
console.log(precio);
// existe otra diferencia ente el let y const
// cuando se trabja con arreglos y objetos, 
